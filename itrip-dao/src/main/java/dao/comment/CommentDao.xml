<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.comment.CommentDao">
<insert id="addComment">
INSERT INTO itrip_comment(hotelId,productId,orderId,productType,content,userId,isHavingImg,positionScore,facilitiesScore,serviceScore,hygieneScore,score,tripMode,isOk,creationDate,createdBy)
VALUES(#{hotelId},#{productId},#{orderId},#{productType},#{content},#{userId},#{isHavingImg},#{positionScore},#{facilitiesScore},#{serviceScore},#{hygieneScore},#{score},#{tripMode},#{isOk},#{creationDate},#{createdBy})
</insert>
</mapper>